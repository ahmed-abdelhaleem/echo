# Placeholder. Real Flutter client scaffold lands in PR 2 (T-CLIENT-001..003).
#
# This is M0 scope deferred per the build plan attached to PR 1:
# - flutter create on this branch would not be reproducible without the Flutter
#   SDK in CI.
# - The acceptance criterion `flutter build apk/ipa/windows/macos` requires
#   macOS + Windows GitHub-hosted runners, which is a paid-runner decision
#   that should be made explicitly.
#
# See docs/07_AI_Agent_Implementation_Guide.md milestone M0 → T-CLIENT-001..003.
