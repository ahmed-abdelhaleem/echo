# Placeholder. tools/trait-replay replays a recorded event log and recomputes
# the trait vector deterministically. Used to detect content changes that
# would shift trait vectors for existing playthroughs. M1 scope (T-ML-010).
