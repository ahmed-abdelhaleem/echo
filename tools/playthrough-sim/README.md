# Placeholder. tools/playthrough-sim is a deterministic playthrough simulator
# used by CI to drive smoke tests end-to-end. M1 scope (T-CONTENT-002 +
# T-CORE-010). Until then `make simulate` is a no-op exit-0 stub per the
# convention in docs/07_AI_Agent_Implementation_Guide.md.
